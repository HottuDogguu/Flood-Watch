<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication"
    android:usesCleartextTraffic="true"
    tools:ignore="MissingLeanbackLauncher">


    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notification permission for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Storage permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!-- For Android 13+ -->

    <!-- Camera and Phone permissions -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Features - marked as not required so app works on all devices -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"
        tools:ignore="PhotoAndVideoPolicy,SelectedPhotoAccess" />
    <!-- Storage permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!-- For Android 13+ -->

    <!-- Camera and Phone permissions -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Features - marked as not required so app works on all devices -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />



    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="Flood Watch"
        android:name=".App"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/round_icon_flood_watch"
        android:supportsRtl="true"
        android:theme="@style/Theme.FloodWatch"
        android:usesCleartextTraffic="true" >

        <activity
            android:name=".ui.activity.auth.LoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Messaging Service -->
        <service
            android:name=".firebase.FirebaseMessagingServices"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- Default Notification Channel ID -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel" />

        <activity
            android:name=".ui.activity.auth.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.auth.UploadProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.auth.SignUpAsGoogleActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.auth.LinkGoogleAccountActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.home.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.home.AnnouncementActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.home.HomeActivity" />

        <activity
            android:name=".ui.activity.home.NotificationHistoryActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.home.HomeActivity" />

        <activity

            android:name=".ui.activity.auth.EmailVerificationActivity"
            android:exported="false" />
        <!-- Announcement Activity -->
        <activity
            android:name=".AnnouncementActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.home.HomeActivity" />

        <!-- Weather Activity -->
        <activity
            android:name=".ui.activity.home.WeatherMonitorActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.home.HomeActivity" />

        <!-- Profile Activity -->
        <activity
            android:name=".ui.activity.home.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:screenOrientation="fullSensor"
            android:parentActivityName=".ui.activity.home.HomeActivity"
            tools:ignore="DiscouragedApi">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.home.HomeActivity" />
        </activity>

        <!-- MDRRMO Contacts Activity -->
        <activity
            android:name=".ui.activity.home.MDRRMOContactsActivity"
            android:exported="false"
            android:label="MDRRMO Contacts"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:parentActivityName=".ui.activity.home.ProfileActivity" />

        <!--News ACtivity-->
         <activity
            android:name=".ui.activity.home.NewsActivity"
            android:exported="false"
            android:label="MDRRMO Contacts"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:parentActivityName=".ui.activity.home.ProfileActivity" />
        <!--News ACtivity-->
        <activity
            android:name=".ui.activity.home.FloodHistoryActivity"
            android:exported="false"
            android:label="MDRRMO Contacts"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:parentActivityName=".ui.activity.home.ProfileActivity" />

        <!-- FileProvider for camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>